<%= form_for(@publishing_company) do |f| %>
  <% if @publishing_company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@publishing_company.errors.count, "error") %> prohibited this publishing_company from being saved:</h2>

      <ul>
      <% @publishing_company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :prefecture_id %><br>
    <%= f.number_field :prefecture_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
